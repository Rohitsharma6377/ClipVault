<Page
    x:Class="ClipVault.Views.ClipboardListPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:u="using:ClipVault.Models">
    <Grid>
        <StackPanel>
            <TextBlock x:Name="HeaderTitle" FontSize="20" Margin="12,8,12,8" Text="Clipboard History" />
            <ListView ItemsSource="{x:Bind ViewModel.Items, Mode=OneWay}">
                <ListView.ItemTemplate>
                    <DataTemplate x:DataType="u:ClipboardItem">
                        <Border Padding="8" Margin="8,4" CornerRadius="8" Background="Transparent">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>

                                <TextBox Text="{x:Bind Content}"
                                         IsReadOnly="True"
                                         TextWrapping="Wrap"
                                         AcceptsReturn="True"
                                         Height="160"
                                         FontFamily="Consolas"
                                         Background="Transparent"
                                         BorderThickness="0"
                                         ScrollViewer.VerticalScrollBarVisibility="Auto"
                                         Grid.Column="0" />

                                <StackPanel Orientation="Vertical" Grid.Column="1" Margin="8,0,0,0">
                                    <Button Content="Copy" Click="CopyButton_Click" Tag="{x:Bind}" Margin="0,0,0,6" />
                                    <Button Content="Pin" Click="PinButton_Click" Tag="{x:Bind}" Margin="0,0,0,6" />
                                    <Button Content="Delete" Click="DeleteButton_Click" Tag="{x:Bind}" />
                                </StackPanel>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </StackPanel>
    </Grid>
</Page>
